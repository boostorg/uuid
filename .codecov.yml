coverage:
  range: "85...100"

fixes:
  - "*/boost-root/boost/::include/boost/"
  - "*/boost-root/libs/uuid/::"

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no
